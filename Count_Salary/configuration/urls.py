"""
URL-маршрути для додатку 'configuration'.

Наразі цей додаток не має публічних сторінок (views), оскільки
всі налаштування керуються через стандартну адмін-панель Django.
"""
from django.urls import path

# Цей список ОБОВ'ЯЗКОВИЙ для того, щоб Django міг підключити цей файл.
# Він може залишатися порожнім, поки в додатку немає власних сторінок.
urlpatterns = [
    # Приклад, як це могло б виглядати в майбутньому,
    # якби ми створили view для редагування налаштувань:
    # path('edit/', views.edit_configuration, name='config-edit'),
]

